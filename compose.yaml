services:
  frontend:
    container_name: course-sphere-frontend
    image: ${REGISTRY}/frontend:latest
    restart: always
    networks:
      - network

networks:
  network:
    name: course-sphere-network
    external: true
